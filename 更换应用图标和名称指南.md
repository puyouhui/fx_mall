# 更换应用图标和名称指南

本指南将帮助您更换 `distribution_app`（分销应用）和 `employees_app`（员工应用）的图标和名称。

## 📱 需要修改的文件位置

### 1. distribution_app（分销应用）

#### 应用名称修改位置：

1. **Android 应用名称**
   - 文件：`distribution_app/android/app/src/main/AndroidManifest.xml`
   - 修改：`android:label="distribution_app"` → 改为您想要的名称

2. **iOS 应用名称**
   - 文件：`distribution_app/ios/Runner/Info.plist`
   - 修改：
     - `CFBundleDisplayName` → 显示在桌面上的名称
     - `CFBundleName` → 内部名称

3. **Web 应用名称**
   - 文件：`distribution_app/web/manifest.json`
   - 修改：`name` 和 `short_name` 字段

4. **应用描述（可选）**
   - 文件：`distribution_app/pubspec.yaml`
   - 修改：`description` 字段

#### 应用图标修改位置：

1. **Android 图标**
   - 目录：`distribution_app/android/app/src/main/res/`
   - 需要替换以下目录中的 `ic_launcher.png`：
     - `mipmap-mdpi/ic_launcher.png` (48x48)
     - `mipmap-hdpi/ic_launcher.png` (72x72)
     - `mipmap-xhdpi/ic_launcher.png` (96x96)
     - `mipmap-xxhdpi/ic_launcher.png` (144x144)
     - `mipmap-xxxhdpi/ic_launcher.png` (192x192)

2. **iOS 图标**
   - 目录：`distribution_app/ios/Runner/Assets.xcassets/AppIcon.appiconset/`
   - 需要准备多种尺寸的图标（建议使用 Xcode 或在线工具生成）

3. **Web 图标**
   - 目录：`distribution_app/web/icons/`
   - 需要替换：
     - `Icon-192.png` (192x192)
     - `Icon-512.png` (512x512)
     - `Icon-maskable-192.png` (192x192)
     - `Icon-maskable-512.png` (512x512)

---

### 2. employees_app（员工应用）

#### 应用名称修改位置：

1. **Android 应用名称**
   - 文件：`employees_app/android/app/src/main/AndroidManifest.xml`
   - 修改：`android:label="employees_app"` → 改为您想要的名称

2. **iOS 应用名称**
   - 文件：`employees_app/ios/Runner/Info.plist`
   - 修改：
     - `CFBundleDisplayName` → 显示在桌面上的名称
     - `CFBundleName` → 内部名称

3. **Web 应用名称**
   - 文件：`employees_app/web/manifest.json`
   - 修改：`name` 和 `short_name` 字段

4. **应用描述（可选）**
   - 文件：`employees_app/pubspec.yaml`
   - 修改：`description` 字段

#### 应用图标修改位置：

1. **Android 图标**
   - 目录：`employees_app/android/app/src/main/res/`
   - 需要替换以下目录中的 `ic_launcher.png`：
     - `mipmap-mdpi/ic_launcher.png` (48x48)
     - `mipmap-hdpi/ic_launcher.png` (72x72)
     - `mipmap-xhdpi/ic_launcher.png` (96x96)
     - `mipmap-xxhdpi/ic_launcher.png` (144x144)
     - `mipmap-xxxhdpi/ic_launcher.png` (192x192)

2. **iOS 图标**
   - 目录：`employees_app/ios/Runner/Assets.xcassets/AppIcon.appiconset/`
   - 需要准备多种尺寸的图标（建议使用 Xcode 或在线工具生成）

3. **Web 图标**
   - 目录：`employees_app/web/icons/`
   - 需要替换：
     - `Icon-192.png` (192x192)
     - `Icon-512.png` (512x512)
     - `Icon-maskable-192.png` (192x192)
     - `Icon-maskable-512.png` (512x512)

---

## 🛠️ 快速操作步骤

### 步骤 1: 准备图标文件

1. 准备一个 1024x1024 像素的 PNG 图标（透明背景）
2. 使用在线工具生成各种尺寸：
   - [App Icon Generator](https://www.appicon.co/)
   - [Icon Kitchen](https://icon.kitchen/)
   - [MakeAppIcon](https://makeappicon.com/)

### 步骤 2: 替换图标文件

#### Android 图标替换：
```bash
# 将生成的图标文件复制到对应的目录
# distribution_app
cp your_icon_48.png distribution_app/android/app/src/main/res/mipmap-mdpi/ic_launcher.png
cp your_icon_72.png distribution_app/android/app/src/main/res/mipmap-hdpi/ic_launcher.png
cp your_icon_96.png distribution_app/android/app/src/main/res/mipmap-xhdpi/ic_launcher.png
cp your_icon_144.png distribution_app/android/app/src/main/res/mipmap-xxhdpi/ic_launcher.png
cp your_icon_192.png distribution_app/android/app/src/main/res/mipmap-xxxhdpi/ic_launcher.png

# employees_app（同样的操作）
cp your_icon_48.png employees_app/android/app/src/main/res/mipmap-mdpi/ic_launcher.png
# ... 以此类推
```

#### iOS 图标替换：
- 使用 Xcode 打开项目，在 `Assets.xcassets` 中替换 AppIcon
- 或者手动替换 `ios/Runner/Assets.xcassets/AppIcon.appiconset/` 目录中的图标

#### Web 图标替换：
```bash
# distribution_app
cp your_icon_192.png distribution_app/web/icons/Icon-192.png
cp your_icon_512.png distribution_app/web/icons/Icon-512.png
cp your_icon_192.png distribution_app/web/icons/Icon-maskable-192.png
cp your_icon_512.png distribution_app/web/icons/Icon-maskable-512.png

# employees_app（同样的操作）
```

### 步骤 3: 修改应用名称

按照上面的文件位置，修改相应的配置文件中的名称字段。

---

## 📝 注意事项

1. **图标格式**：必须使用 PNG 格式，支持透明背景
2. **图标尺寸**：确保使用正确的尺寸，否则可能显示模糊
3. **iOS 图标**：iOS 需要多种尺寸，建议使用 Xcode 或专业工具生成
4. **应用名称长度**：建议不超过 12 个字符，避免在手机上显示不全
5. **修改后重新构建**：修改图标和名称后，需要重新构建应用才能看到效果

---

## 🔄 重新构建应用

修改完成后，需要重新构建应用：

```bash
# 清理构建缓存
cd distribution_app
flutter clean
flutter pub get

# 重新构建
flutter build apk  # Android
flutter build ios  # iOS
flutter build web  # Web

# employees_app 同样操作
cd ../employees_app
flutter clean
flutter pub get
flutter build apk
```

---

## 💡 提示

- 如果只需要修改名称，可以只修改配置文件，不需要替换图标
- 如果只需要修改图标，可以只替换图标文件，保持名称不变
- 建议使用专业的图标设计工具确保图标质量
- 测试时可以在模拟器或真机上验证图标和名称是否正确显示

